import React from 'react';
import ReactDOM from 'react-dom';
import LoginPage from './views/loginSystem/loginPage';

ReactDOM.render(
  <React.StrictMode>
    <LoginPage />
  </React.StrictMode>,
  document.getElementById('root')
);


